{#
| Variable     | Type                  | Description |
|--------------|-----------------------|-------------|
| page         | Page                  |             |
| page.title   | string                |             |
| page.content | string                |             |
| page.url     | string                |             |
| page.slug    | string                |             |
| page.type    | string                |             |
#}
{% extends "layouts.master" %}
{% block content %}
    {% if page.type == 'customised' %}
      {#
          Hook: information_page.information_page
          Fires before the main informative page content section.
          Use this hook to inject custom content or components above the page content area.
      #}
      <div class="s-blocks-wrapper s-information-page !mt-0">{% hook 'information_page.information_page' %}</div>
    {% else %}
      <div class="container">
          {# add breadcumbs container in pages to make a space in case breadcrumbs is off #}
          <nav class="breadcrumbs w-full py-5">
              <salla-breadcrumb></salla-breadcrumb>
          </nav>

          {# TESTAHLEEN editorial page: quiet single column on the ivory ground #}
          <article class="tsx-page">
              <header class="tsx-page-head">
                  <h1 class="tsx-page-title">{{ page.title }}</h1>
              </header>
              <div class="tsx-page-body content-entry">
                  {{ page.content|raw }}
              </div>
              <salla-comments item-id="{{page.id}}" type="page"></salla-comments>
          </article>
      </div>
    {% endif %}
{% endblock %}
